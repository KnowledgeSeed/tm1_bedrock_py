# év összesen szabály
['Sales Channel And Loyalty Measure':'Value'] = C:
IF( ELLEV('Period',!Period)>0,
   DB('Sales Channel And Loyalty', !Version, DB('}ElementAttributes_Period', !Period, 'Last_M_Y'),
   !Loyalty, !Sales Channel, !Product Category, !Subscription Plan Movement, !Sales Channel And Loyalty Measure),
STET);

# verziószabály
[]= N:
IF(ELISPAR('Version','Report',!Version) = 1,
STET,Continue);



