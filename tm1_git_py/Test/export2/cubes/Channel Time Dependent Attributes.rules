

SKIPCHECK;

# Módos Dávid 2024 03 17
['Channel Time Dependent Attributes Measure':'Jogdíj'] = C:
IF( ELLEV('Period',!Period)>0,
  DB('Channel Time Dependent Attributes', !Version, !Period | '12', !Channel, !Channel Time Dependent Attributes Measure),
Continue);


[]= N:
IF(ELISPAR('Version','Report',!Version) = 1,
STET,Continue);

# ha a currency éves legyen az utolsó hónapja
['Channel Time Dependent Attributes Measure':'Currency'] = S:
IF( ELLEV('Period',!Period)>0,
  DB('Channel Time Dependent Attributes', !Version, !Period | '12', !Channel, !Channel Time Dependent Attributes Measure),
STET);

['Channel Time Dependent Attributes Measure':'Payment Type'] = S:
IF( ELLEV('Period',!Period)>0,
  DB('Channel Time Dependent Attributes', !Version, !Period | '12', !Channel, !Channel Time Dependent Attributes Measure),
STET);



FEEDERS;

['Channel Time Dependent Attributes Measure':'EFI'] =>
DB('Channel Cost', !Version, !Period, !Channel, 'Total Product', 'Currency Total', 'EFI Cost', 'Value');


['Channel Time Dependent Attributes Measure':'EFI Cost'] =>
DB('Channel Cost', !Version, !Period, !Channel, 'Total Product', 'Currency Total', 'EFI Cost', 'Value');

['Channel Time Dependent Attributes Measure':'Currency'] =>
DB('Channel Cost', !Version, !Period, !Channel, '214', 'Currency Total', 'EFI Cost', 'Value');

['Channel Time Dependent Attributes Measure':'Currency'] =>
DB('Channel Cost', !Version, !Period, !Channel, 'Total Product', 'Currency Total', 'EFI Cost', 'Value');


['Channel Time Dependent Attributes Measure':'Jogdíj'] =>
DB('Channel Cost', !Version, !Period, !Channel, 'Total Product', 'HUF', 'Royalty EFI Cost', 'Value');


['Channel Time Dependent Attributes Measure':'Flat'] =>
DB('Channel Cost', !Version, !Period, !Channel, 'Total Product', 'Currency Total', 'cpsReport', 'Value');