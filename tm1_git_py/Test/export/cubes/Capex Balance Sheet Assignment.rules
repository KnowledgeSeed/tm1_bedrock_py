#SKIPCHECK;

# verzió szabályok
[]= N:
IF(ELISPAR('Version','Report',!Version) = 1,
STET,Continue);

['Capex Balance Sheet Assignment Measure':'Value'] =N:
DB('Capex', !Version, !Period, !Capex Ledger, 'Value')
*
['Capex Balance Sheet Assignment Measure':'Assignment'];

#FEEDERS;